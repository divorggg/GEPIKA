<font-family xmlns:app="http://schemas.android.com/apk/res-auto">
    <font
        app:fontStyle="normal"
        app:fontWeight="400"
        app:font="@font/poppins" />

    <font
        app:fontStyle="italic"
        app:fontWeight="400"
        app:font="@font/poppins" />

    <font
        app:fontStyle="normal"
        app:fontWeight="700"
        app:font="@font/poppins_bold" />

    <!-- Tambahkan style font lainnya jika diperlukan -->
</font-family>